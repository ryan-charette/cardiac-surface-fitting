# Data

`data/sample` contains the small tube and bifurcation T-mesh examples used by
the tests, examples, and benchmarks.

`data/la` contains the trimmed left-atrium end-diastole and end-systole point
clouds used by `benchmarks/benchmark_la_memory.py`.

Large cardiac frame data from the original archive is not copied into this
cleaned repository.
